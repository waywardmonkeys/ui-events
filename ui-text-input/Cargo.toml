[package]
name = "ui-text-input"
version.workspace = true
license.workspace = true
edition.workspace = true
description = "Host-side text-input state and query traits for backend adapters."
keywords = ["ui", "text", "ime", "input"]
categories = ["gui"]
repository.workspace = true
rust-version.workspace = true

[package.metadata.docs.rs]
all-features = true
default-target = "x86_64-unknown-linux-gnu"
targets = []

[features]
default = ["std"]
std = ["dpi/std", "ui-events/std"]

[dependencies]
dpi.workspace = true
ui-events = { workspace = true, default-features = false }

[lints]
workspace = true
